spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: customer

server:
  port: 8092

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  tracing:
    sampling:
      probability: 1